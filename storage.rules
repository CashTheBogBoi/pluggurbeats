rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Backend torn down for a full rebuild. Deny all access until the new
    // upload/serving model is defined.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
