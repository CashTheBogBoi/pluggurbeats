rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Backend torn down for a full rebuild. Nothing reads or writes Firestore
    // from the client right now, so deny everything. Rebuild rules alongside
    // the new data model.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
